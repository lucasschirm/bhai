// Populated by TASK_0019 (WebLLM driver plugin).
// Peer dependency `@mlc-ai/web-llm` is declared only alongside this subpath
// (TASK_0019's job) and the engine is injected at runtime, never statically imported.
export {}
