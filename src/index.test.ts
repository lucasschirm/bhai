import { describe, expect, it } from "vitest"

// Placeholder test only. Exists solely to keep `pnpm test` green until TASK_0002
// adds real type/unit tests. Delete this file once real tests land.
describe("placeholder", () => {
	it("is true", () => {
		expect(true).toBe(true)
	})
})
